import React from 'react';
import styles from './ModalBackground.module.scss';

const BackgroundModal = (): React.JSX.Element => {
    return <div className={styles.background}></div>;
};

export default BackgroundModal;
